import { ErrorsType } from 'src/common/common.interfaces';

export const FileErrorMessages = {
	FILE_EXIST: {
		ru: 'Файл с таким именем уже существует',
		en: 'A file with that name already exists'
	}
} satisfies ErrorsType;
